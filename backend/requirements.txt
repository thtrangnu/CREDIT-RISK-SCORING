# Backend layer, installed on top of ml/requirements.txt. The backend loads ml/ artifacts,
# so it must use the SAME numpy/pandas/sklearn/lightgbm versions: pickles and model.txt are
# not guaranteed to stay compatible across major versions.
#
#   -r ../ml/requirements.txt  keeps a single source of versions and prevents drift.
-r ../ml/requirements.txt

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
sqlalchemy>=2.0,<3.0
pymysql>=1.1,<2.0
alembic>=1.13,<2.0
python-multipart>=0.0.9
httpx>=0.27          # required by FastAPI TestClient
