# Tầng backend. Cài chồng lên ml/requirements.txt (backend load artifact của ml/
# nên phải dùng CÙNG bộ version numpy/pandas/sklearn/lightgbm — pickle và
# model.txt không đảm bảo tương thích ngược qua các major version).
#
#   -r ../ml/requirements.txt  giữ 1 nguồn version duy nhất, tránh drift giữa 2 tầng.
-r ../ml/requirements.txt

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
sqlalchemy>=2.0,<3.0
pymysql>=1.1,<2.0
alembic>=1.13,<2.0
python-multipart>=0.0.9
httpx>=0.27          # TestClient của FastAPI
