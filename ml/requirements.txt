# ML layer (training + explain). The upper bounds are deliberate:
#
#   numpy<2.5  - MANDATORY. shap depends on numba, and numba only supports NumPy <= 2.4.
#                On numpy 2.5, `import shap` dies immediately with
#                "ImportError: Numba needs NumPy 2.4 or less".
#
# Install: python -m venv .venv && .venv/bin/pip install -r ml/requirements.txt
numpy>=1.26,<2.5
pandas>=2.2,<3.0
scikit-learn>=1.5,<2.0
lightgbm>=4.5,<5.0
shap>=0.46,<0.50
pyarrow>=17
pyyaml>=6.0
pytest>=8.0
